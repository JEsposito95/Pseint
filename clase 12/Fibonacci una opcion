Algoritmo ejer12fibo
	definir n Como Entero
	Escribir "Escribir un número"
	leer n
	Escribir fibonacci(n)
FinAlgoritmo
Funcion r<-fibonacci(x)
	//definir ,i,j Como Entero
	definir k,i,j,c Como Real

	Escribir "El lugar ",x," de serie"
	j=0
	k=1
	para i<-1 Hasta x-1
		Escribir Sin Saltar j, " , "
		c=j+k
		j=k
		k=c
	FinPara
	para i<-x hasta x
		Escribir Sin Saltar j
		c=j+k
		j=k
		k=c
	FinPara

FinFuncion
//F(n)=((z^n)-(-z^(-n))/raiz(5)
//j=((z^i)-(-z^(-i)))/raiz(5)
//z=(1+raiz(5))/raiz(5)